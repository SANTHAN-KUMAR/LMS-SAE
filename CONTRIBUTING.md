## Contributing

- Use Python 3.11+ and follow PEP8.
- Create branches from `main` and open small PRs.
- Add tests under `tests/` for any new functionality.
- Run `pytest` locally before pushing.
- Do not commit secrets; use `.env` locally and `.env.example` for docs.
